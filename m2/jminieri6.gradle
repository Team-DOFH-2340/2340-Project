apply plugin: 'java'

dependencies {
	 compile name: 'resources'
}

repositories {
   flatDir {
       dirs 'lib'
   }
}

jar {
	manifest {
    attributes("Implementation-Title": "Gradle",
                   "Implementation-Version": "version")
	}
}
